{
  "name": "v7-adaptive-learning",
  "version": "7.0.0",
  "private": true,
  "type": "module",
  "description": "Adaptive AI anti-bot and traffic policy system for Cloudflare Workers",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply DB --local",
    "d1:migrate:remote": "wrangler d1 migrations apply DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
